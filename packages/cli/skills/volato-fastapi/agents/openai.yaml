interface:
  display_name: "Volato FastAPI calibration"
  short_description: "Install and verify the private FastAPI HTTP Errors candidate."
  default_prompt: "Use $volato-fastapi to install and verify the private Volato Errors calibration in this FastAPI application."
