interface:
  display_name: "Detect PMF"
  short_description: "Compare outcome-led PMF signals by branch"
  default_prompt: "Use $detect-pmf to map, instrument, and compare behavioral product outcomes with founder-approved branches."
